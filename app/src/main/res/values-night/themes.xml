<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (Light Mode). -->
    <style name="Base.Theme.News_Application" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/action</item>
        <item name="colorPrimaryVariant">@color/action2</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="Theme.News_Application" parent="Base.Theme.News_Application" />
</resources>